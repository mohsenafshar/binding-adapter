apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    implementation 'com.squareup:javapoet:1.11.1'
    compile project(path: ':adapterannotation')
}

sourceCompatibility = "8"
targetCompatibility = "8"
